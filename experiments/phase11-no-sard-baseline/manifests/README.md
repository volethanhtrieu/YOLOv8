# Manifest publication

`phase11_no_sard_manifest.csv` is the sanitized manifest for all 4,844
adjudicated images. Absolute server paths were removed and derived paths are
relative to the dataset root. The manifest retains source, grouping, split,
provenance hashes, and negative-image status.

Its SHA256 is recorded in `../reports/artifact_sha256.txt`.
